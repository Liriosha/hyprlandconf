return {
	browser = "xdg-open https:", -- This little hack should always open the default browser
	fileManager = "nautilus",
	menu = "pkill fuzzel || fuzzel --config=.config/hypr/fuzzel/fuzzel.ini",
	terminal = "kitty",
}
